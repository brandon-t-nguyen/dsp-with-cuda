#include <cuda.h>
#include <cuda_runtime.h>

#include <operations.h>

std::vector<double> * Operations::convolve_gpu( const std::vector<double> * x,
                                                const std::vector<double> * h )
{
    return 0x0;
}
